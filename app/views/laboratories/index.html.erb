<h3>Pregled laboratorija</h3>

<table class="table table-stripped">
  <thead>
    <tr>
      <th>Naziv</th>
      <th>Lokacija</th>
      <th>Telefon</th>
      <th>Email</th>
      <th></th>
    </tr>
  </thead>
<% @laboratories.each do |laboratory| %>
  <tr>
    <td><%= laboratory.name %></td>
    <td><%= laboratory.municipality.municipality if laboratory.municipality_id %></td>
    <td><%= laboratory.phone %></td>
    <td><%= laboratory.email %></td>
    <td>
        <%= link_to 'Uredi', edit_laboratory_path(laboratory) %> | 
        <%= link_to 'Izbriši', laboratory, method: :delete, data: { confirm: 'Jeste li sigurni?' } %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova laboratorija', new_laboratory_path %>
